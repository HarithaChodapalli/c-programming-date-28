#include<stdio.h>
main()
{
	int n,r,sum=0,temp,a,b,i;
	printf("enter the number:");
	scanf("%d",&n);
	temp=n;
     n=i;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(temp=sum)
	printf("an armstrong number");
	else
	printf("not an armstrong number");
}}
